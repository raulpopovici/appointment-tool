// import { useUser } from '../../hooks/useUser';
// import { mockServices } from '../../mocks/mockServices';

// export const Provider = () => {
//   const { selectedProvider } = useUser();
//   const services = mockServices;
//   const provider = services.find((service) => service.id === selectedProvider);

//   return <div>{provider?.category}</div>;
// };
